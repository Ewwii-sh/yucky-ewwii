(defvar my_variable "20")

(defwidget my_bar [arg1]
  (box :orientation "h"
    (label :text "${my_variable == 20 ? 'hi' : 'bye'}")))

(defwindow top_bar
  :monitor 0
  :geometry (geometry :x "0%" :y "0%" :width "100%" :height "30px")
  (my_bar :arg1 "Hello World"))